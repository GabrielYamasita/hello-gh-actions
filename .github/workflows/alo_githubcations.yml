name: Exemplo de Fluxo de Trabalho

on:
  workflow_dispatch:

jobs:
  acao_inicial_job:
    runs-on: ubuntu-latest
    steps:
      - name: Imprime OLA
        run: echo "Hello, world!"
        
      - name: Imprime TCHAU
        run: echo "Bye!"
